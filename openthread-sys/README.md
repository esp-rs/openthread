# openthread-sys

"No-thrills" raw Rust bindings to the OpenThread library.
`no_std` compatible and thus suitable for use on baremetal MCUs.

## Goal

Power `openthread` - a **type-safe**, Rust API around the raw bindings exposed by this sys crate

## More details

For more information, consult the README of the `openthread` crate.
